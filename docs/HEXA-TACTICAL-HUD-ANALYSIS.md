# 🎮 Hexa-Tactical OS 98 HUD - 기술 분석 문서

> **버전**: v1.0  
> **작성일**: 2026-01-23  
> **프리셋 ID**: `hexa-tactical`

---

## 📋 개요

**Hexa-Tactical OS 98 HUD**는 Windows 98 스타일의 홀로그래픽 게임 UI를 React로 구현한 HUD 프리셋입니다.  
"몽중게임" 프로젝트의 줄콘티 Scene 8~12를 지원하며, 레퍼런스 이미지 99.99% 일치를 목표로 제작되었습니다.

### 주요 특징
- 🖥️ **Win98 스타일** - 타이틀바, 컨트롤 버튼, 스캔라인 등 레트로 UI
- 🔶 **육각형(Hexagon) 디자인** - 포트레이트, 배경 그리드
- ✨ **골드 컬러 시스템** - 시네마틱한 골드 글로우 효과
- 🎬 **9개 시나리오** - 상황별 테마 전환 (idle → combat → infected 등)
- ⌨️ **인터랙션** - 키보드/마우스 이벤트 기반 UI 반응
- 🔥 **PNG 시퀀스 효과** - 368프레임 불 애니메이션

---

## 🛠️ 기술 스택

### Core Framework
| 기술 | 버전 | 용도 |
|------|------|------|
| **React** | ^19.2.0 | UI 컴포넌트 프레임워크 |
| **TypeScript** | ~5.9.3 | 타입 안전성 |
| **Vite** | ^7.2.4 | 빌드/개발 서버 |

### Animation & Effects
| 기술 | 버전 | 용도 |
|------|------|------|
| **Framer Motion** | ^12.27.5 | React 애니메이션 (fade, scale, slide) |
| **Canvas API** | Native | 육각형/차트/연결라인 드로잉 |
| **CSS Animations** | Native | 스캔라인 스크롤, 글로우 펄스 |

### Rendering & Export
| 기술 | 버전 | 용도 |
|------|------|------|
| **Remotion** | ^4.0.407 | 프레임 정확 비디오 렌더링 |
| **@remotion/player** | ^4.0.407 | 웹 내 프리뷰 플레이어 |
| **html-to-image** | ^1.11.13 | DOM → PNG 캡처 (레거시) |

### State Management
| 기술 | 버전 | 용도 |
|------|------|------|
| **Zustand** | ^5.0.10 | 전역 상태 관리 |
| **React Context** | Native | 컴포넌트 간 상태 공유 |

### Styling
| 기술 | 용도 |
|------|------|
| **CSS-in-JS (Inline)** | 컴포넌트별 동적 스타일 |
| **TailwindCSS** | 유틸리티 클래스 (부분 사용) |
| **CSS Variables** | 테마 색상 시스템 |

---

## 📁 폴더 구조

```
src/presets/hexaTactical/
├── index.ts                    # 모듈 export
├── HexaTacticalHUD.tsx         # 🎯 메인 HUD 컴포넌트 (2500+ lines)
├── constants.ts                # 상수, 테마, 시나리오 정의
├── styles.ts                   # 공통 스타일 헬퍼
├── components/                 # UI 컴포넌트
│   ├── WindowShell.tsx         # Win98 윈도우 프레임
│   ├── DualHexPortrait.tsx     # 듀얼 육각형 포트레이트 (Canvas)
│   ├── ScanlineOverlay.tsx     # CRT 스캔라인 효과
│   ├── ConnectionBanner.tsx    # "CONNECTION COMPLETE" 배너
│   ├── HexBackdrop.tsx         # 육각형 배경 그리드
│   └── GameIcons.tsx           # 게임 아이콘 SVG 모음
├── effects/                    # 시각 효과
│   ├── FireSequence.tsx        # 🔥 368프레임 PNG 불 애니메이션
│   ├── TextDecipher.tsx        # 글리치 텍스트 해독 효과
│   ├── MovingLines.tsx         # 배경 이동 라인 (Canvas)
│   ├── Illuminator.tsx         # 조명 글로우 효과
│   └── FrameCorners.tsx        # 모서리 프레임 효과
└── utils/
    └── imageHelper.ts          # 이미지 경로 헬퍼 (Remotion 호환)
```

---

## 🎨 디자인 시스템

### 색상 시스템 (constants.ts)

```typescript
export const HUD_COLORS = {
  // 🔶 Gold System (메인 테마)
  gold: '#D4A017',              // 기본 테두리/텍스트
  goldBright: '#FFD700',        // 강조/아이콘
  goldDim: '#8B7500',           // 비활성
  goldGlow: 'rgba(255,215,0,0.35)', // 글로우

  // 🖤 Background System
  bgDark: '#0A0A08',            // 거의 검정
  bgPanel: '#0D0D0A',           // 패널 배경
  
  // 🔴🟢🔵🟣 Status Colors
  red: '#FF3333',               // 위험/데미지
  green: '#00FF66',             // 감염/회복
  cyan: '#00D4FF',              // 접속/링크
  purple: '#9900FF',            // 심리 공격
}
```

### 9개 시나리오 테마

| ID | 이름 | 주요 색상 | 용도 |
|----|------|----------|------|
| `idle` | 기본 탐색 | Gold | 평상시 |
| `link_progress` | 접속 진행 | Cyan | 블랙룸 진입 |
| `persona_sync` | 페르소나 동기화 | Cyan→Gold | 접속 완료 전환 |
| `profile_danger` | 프로필 위험 | Gold+Red | 위험 경고 |
| `monster_combat` | 몬스터 전투 | Red | 랩틸리언 전투 |
| `psycho_attack` | 심리 공격 | Purple | 수빈사랑 미래 분기 |
| `infected` | 감염 상태 | Green/Purple | 매지코 부패 |
| `trauma` | 트라우마 | Grey | 과거 상처 |
| `evolved` | 진화 완료 | Gold+White | 초진화 |

### 폰트 시스템

```typescript
export const FONTS = {
  ui: '"Outfit", "AstaSans", "Rajdhani", system-ui',      // UI 기본
  display: '"Outfit", "Orbitron", "Rajdhani", sans-serif', // 디스플레이
  mono: '"JetBrains Mono", "Fira Code", monospace',        // 터미널
  korean: '"Do Hyeon", "Noto Sans KR", sans-serif',        // 한글
}
```

---

## 🏗️ 핵심 컴포넌트 분석

### 1. HexaTacticalHUD.tsx (메인 컴포넌트)

**역할**: HUD 전체를 관리하는 루트 컴포넌트

```
┌─────────────────────────────────────────────────────────────┐
│ HexaTacticalHUD                                             │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ State Management (useState, useRef)                     │ │
│ │ - scenario, player, skills, targets, hitMarkers...      │ │
│ └─────────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Event Handlers                                          │ │
│ │ - handleKeyDown (1-9: 시나리오, D: 데미지, `: 온보딩)   │ │
│ │ - handleMouseMove, handleMouseDown, handleMouseUp       │ │
│ └─────────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Render (JSX)                                            │ │
│ │ - ScanlineOverlay (전역 CRT 효과)                       │ │
│ │ - PlayerStatusWidget (왼쪽 상단)                        │ │
│ │ - TeamStatusWidget (오른쪽)                             │ │
│ │ - SkillBar (하단)                                       │ │
│ │ - OnboardingPopup (중앙 모달)                           │ │
│ │ - TargetLock, HitMarker, DamageArrow (동적 UI)         │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

**키보드 매핑**:
- `1-9`: 시나리오 전환
- `` ` ``: 온보딩 팝업 시작
- `D`: 데미지 받기 (체력 감소)
- `K`: 즉사 (사망 상태)
- `M`: 몬스터 모드 토글
- `F/G`: 불 효과 토글

**마우스 이벤트**:
- `좌클릭 홀드`: 타겟 박스 생성
- `우클릭`: 히트 마커 + 데미지 화살표

### 2. WindowShell.tsx (Win98 윈도우)

**역할**: Win98 스타일 윈도우 프레임 제공

```
┌─[WINDOW TITLE]─────────[─][□][✕]┐  ← 타이틀바 (골드 그라데이션)
│░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░│  ← 스캔라인 오버레이
│                                 │
│         {children}              │  ← 컨텐츠 영역
│                                 │
└─────────────────────────────────┘  ← 글래스 효과 + 테두리
```

**주요 Props**:
```typescript
interface WindowShellProps {
  title: string
  width?: number | string
  height?: number | string
  borderColor?: string    // 테두리 색상 (테마별)
  titleColor?: string     // 타이틀 텍스트 색상
  glowIntensity?: 'none' | 'soft' | 'normal' | 'strong'
  showControls?: boolean  // ─□✕ 버튼
}
```

**스타일링 특징**:
- 다층 `box-shadow`로 inner glow + outer glow
- `backdrop-filter: blur()` 글래스 효과
- 스캔라인 패턴 (`repeating-linear-gradient`)
- 타이틀바 골드 그라데이션

### 3. SegmentedBar (체력/에너지 바)

**역할**: 세그먼트로 나뉜 체력/에너지 표시

```
HEALTH ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░░░░ 75%
       ↑ 활성 세그먼트   ↑ 비활성
```

**애니메이션 효과**:
1. **마운트 시**: 왼쪽→오른쪽 순차적으로 채워짐
2. **데미지 시**: 빨간색 깜빡임 (5회)
3. **불 효과 시**: 주황색 글로우 순차적 확산

```typescript
const SegmentedBar: React.FC<{ 
  value: number      // 현재 값
  max: number        // 최대 값
  color: string      // 세그먼트 색상
  segments?: number  // 세그먼트 수 (기본 20)
  fireGlow?: boolean // 불 효과 글로우
}>
```

### 4. FireSequence.tsx (불 PNG 시퀀스)

**역할**: 368장의 PNG 이미지를 순차 재생하여 불 애니메이션 구현

```typescript
export const FireSequence: React.FC<{
  playing?: boolean      // 재생 여부
  fps?: number           // 프레임 레이트 (기본 30)
  totalFrames?: number   // 총 프레임 수 (368)
  loop?: boolean         // 루프 여부
  blendMode?: 'screen' | 'add'  // 검은 배경 제거용
  startTimestamp?: number // Remotion 동기화용
}>
```

**동작 방식**:
- **일반 모드**: `setInterval`로 프레임 진행
- **렌더 모드**: `useExternalTimestamp()` Context로 Remotion 프레임과 동기화

**이미지 경로**: `/fire-seq/fire_0000.png` ~ `/fire-seq/fire_0367.png`

### 5. TextDecipher.tsx (글리치 텍스트)

**역할**: 텍스트가 랜덤 문자에서 실제 텍스트로 "해독"되는 효과

```
시작: "Xk#m_9Pp*@"
진행: "CONn3#T-9N"
완료: "CONNECTION"
```

**알고리즘**:
1. Fisher-Yates 셔플로 랜덤 인덱스 생성
2. `requestAnimationFrame`으로 진행
3. 각 프레임마다 일부 문자를 실제 문자로 교체

---

## 🎬 렌더링 파이프라인

### Remotion 통합 구조

```
┌─────────────────────────────────────────────────────────────┐
│ 1. 녹화 (Recording)                                         │
│    HexaTacticalHUD → onStateUpdate → hudStateLog[]          │
│    (33ms 간격으로 상태 스냅샷 저장)                          │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. Remotion Composition                                     │
│    HUDOnlyComposition / HUDOverlayComposition               │
│    - hudStateLogJson (props)                                │
│    - useCurrentFrame() → timestampMs → interpolateHUDState  │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. 렌더링 (Remotion CLI)                                    │
│    npx remotion render HUDOnly --image-format png --seq     │
│    → out/frames/frame_00000.png ~ frame_XXXXX.png           │
└─────────────────────────────────────────────────────────────┘
```

### Context 시스템

```typescript
// 렌더 모드 감지
const RenderModeContext = React.createContext(false)
export const useIsRenderMode = () => React.useContext(RenderModeContext)

// 외부 타임스탬프 (Remotion 프레임 동기화)
const ExternalTimestampContext = React.createContext(0)
export const useExternalTimestamp = () => React.useContext(ExternalTimestampContext)

// 이미지 경로 (Remotion staticFile 호환)
const ImagePathContext = React.createContext<Record<string, string>>({})
export const useImagePath = (key: string, fallback: string) => {
  const paths = React.useContext(ImagePathContext)
  return paths[key] || fallback
}
```

---

## ⌨️ 인터랙션 시스템

### 키보드 단축키

| 키 | 기능 | 코드 위치 |
|----|------|----------|
| `1-9` | 시나리오 전환 | `SCENARIO_KEYS` |
| `` ` `` | 온보딩 시작 (code→voice→enter→...) | `handleKeyDown` |
| `D` | 데미지 받기 (HP -100) | `handleKeyDown` |
| `K` | 즉사 (isDead = true) | `handleKeyDown` |
| `M` | 몬스터 모드 토글 | `handleKeyDown` |
| `F` | 루비안 불 효과 토글 | `handleKeyDown` |
| `G` | 수빈사랑 불 효과 토글 | `handleKeyDown` |

### 마우스 이벤트

```typescript
// 좌클릭 홀드 → 타겟 박스
handleMouseDown = (e) => {
  if (e.button === 0) {
    activeTarget = { x: e.clientX, y: e.clientY, id: Date.now() }
  }
}

// 우클릭 → 히트 마커 + 데미지 화살표
handleMouseDown = (e) => {
  if (e.button === 2) {
    hitMarkers.push({ x, y, id })
    damageArrows.push({ angle, id })
  }
}
```

---

## 🔧 개발 가이드

### 새 시나리오 추가

1. `constants.ts`에 시나리오 ID 추가:
```typescript
export type HexaScenarioId = 
  | 'idle' 
  | 'new_scenario'  // 추가
  | ...
```

2. 테마 정의:
```typescript
export const SCENARIO_THEME: Record<HexaScenarioId, ThemeOverride> = {
  new_scenario: {
    primary: '#FF00FF',
    primaryGlow: 'rgba(255,0,255,0.4)',
    bg: 'rgba(20,0,20,0.85)',
    text: 'rgba(255,200,255,0.92)',
  },
  ...
}
```

3. 시나리오 메타데이터:
```typescript
export const SCENARIOS = {
  new_scenario: {
    name: 'NEW SCENARIO',
    koreanName: '새 시나리오',
    description: '설명',
    shortcut: '0',
  },
  ...
}
```

### 새 컴포넌트 추가

1. `components/` 또는 `effects/`에 파일 생성
2. `constants.ts`의 색상/폰트 상수 사용
3. `useIsRenderMode()` 체크 (타이머 비활성화)
4. `index.ts`에 export 추가

### 렌더링 호환성 체크리스트

- [ ] `setInterval` / `setTimeout` 사용 시 `useIsRenderMode()` 체크
- [ ] 이미지 경로는 `useImagePath()` 사용
- [ ] PNG 시퀀스는 `useExternalTimestamp()` 동기화
- [ ] `framer-motion` 애니메이션 duration 고려

---

## 📊 성능 최적화

### useMemo / useCallback
- 스타일 객체: `useMemo`로 메모이제이션
- 이벤트 핸들러: `useCallback`으로 래핑
- 테마 계산: `useMemo(() => getScenarioTheme(scenario), [scenario])`

### Canvas vs DOM
- **Canvas 사용**: 육각형, 차트, 연결 라인 (DualHexPortrait, MovingLines)
- **DOM 사용**: 윈도우, 텍스트, 버튼 (WindowShell, SegmentedBar)

### 이미지 프리로드
```typescript
useEffect(() => {
  for (let i = 0; i < Math.min(30, totalFrames); i++) {
    const img = new Image()
    img.src = getFireImagePath(i)
  }
}, [])
```

---

## 🚀 빌드 & 배포

### 개발 서버
```bash
npm run dev          # Vite 개발 서버 (5173)
npm run dev:all      # Vite + Remotion 렌더 서버 (5173 + 3456)
```

### Remotion 렌더링
```bash
npm run remotion:studio     # Remotion Studio (미리보기)
npm run remotion:render:png # PNG 시퀀스 출력
npm run remotion:render:mp4 # MP4 비디오 출력
```

### 프로덕션 빌드
```bash
npm run build        # dist/ 폴더에 빌드
npm run preview      # 빌드 결과 미리보기
```

---

## 📝 참고 자료

- **ARWES** (https://arwes.dev/) - Sci-fi UI 프레임워크 (TextDecipher, MovingLines 참고)
- **Framer Motion** (https://motion.dev/) - React 애니메이션
- **Remotion** (https://remotion.dev/) - React 비디오 렌더링

---

## 🎬 렌더링 문제 해결: 프레임 동기화 시스템

### 문제점

```
❌ 현재 문제:
┌─────────────────────────────────────────────────────────────┐
│ Remotion 렌더링 (프레임 독립적)                              │
│                                                             │
│ Frame 0 → 컴포넌트 마운트 → animate={{ opacity: 0→1 }}     │
│ Frame 1 → 컴포넌트 마운트 → animate={{ opacity: 0→1 }}     │
│ Frame 2 → 컴포넌트 마운트 → animate={{ opacity: 0→1 }}     │
│                                                             │
│ 결과: 매 프레임 "시작 상태"만 캡처됨 → 애니메이션 끊김      │
└─────────────────────────────────────────────────────────────┘
```

Framer Motion은 **시간 기반** 애니메이션이지만, Remotion은 **프레임 독립적**으로 렌더링합니다.  
각 프레임이 별도의 React 렌더 사이클이므로, 애니메이션 상태가 유지되지 않습니다.

### 해결책: Event Sourcing + Frame Calculation

```
✅ 해결책:
┌─────────────────────────────────────────────────────────────┐
│ 1. 녹화 시: 애니메이션 시작 시간 기록 (hudStateLog)          │
│    { timestamp: 500, animationStarts: { popup: 500 } }      │
│                                                             │
│ 2. 렌더 시: (현재 프레임 시간 - 시작 시간) / 지속 시간       │
│    Frame 15 (500ms) → progress = (500-500)/300 = 0%         │
│    Frame 24 (800ms) → progress = (800-500)/300 = 100%       │
│                                                             │
│ 3. progress 값으로 style 직접 계산                          │
│    opacity: 0 + (1-0) * progress                            │
│    transform: translateY(${20 * (1-progress)}px)            │
└─────────────────────────────────────────────────────────────┘
```

### 새로운 컴포넌트: FrameAnimatedDiv

`utils/frameAnimation.tsx`에 구현된 프레임 동기화 애니메이션 컴포넌트:

```typescript
import { FrameAnimatedDiv } from './utils/frameAnimation'

// 사용 예시
<FrameAnimatedDiv
  startTime={500}      // 애니메이션 시작 시간 (ms)
  duration={300}       // 애니메이션 지속 시간 (ms)
  from={{ opacity: 0, y: 20 }}
  to={{ opacity: 1, y: 0 }}
  easing="easeOutCubic"
>
  <PopupContent />
</FrameAnimatedDiv>
```

**동작 방식:**
- **일반 모드**: Framer Motion 사용 (부드러운 인터랙션)
- **렌더 모드**: `useExternalTimestamp()`로 프레임 시간 → progress 계산 → style 직접 적용

### 마이그레이션 가이드

```typescript
// ❌ 기존 (Framer Motion)
<motion.div
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.3 }}
>

// ✅ 변환 (Frame Synchronized)
<FrameAnimatedDiv
  startTime={animationStartTime}  // hudStateLog에서 가져옴
  duration={300}
  from={{ opacity: 0, y: 20 }}
  to={{ opacity: 1, y: 0 }}
>
```

### hudStateLog 확장

애니메이션 시작 시간을 기록하도록 `HUDStateSnapshot` 확장:

```typescript
interface HUDStateSnapshot {
  timestamp: number;
  mouse: { x: number; y: number; buttons: number };
  customData?: {
    // 기존 필드...
    
    // 🆕 애니메이션 시작 시간
    animationStarts?: {
      playerStatusWidget?: number;
      teamStatusWidget?: number;
      onboardingPopup?: number;
      monsterBanner?: number;
      hitMarker_1?: number;
      damageArrow_1?: number;
      // ...
    }
  };
}
```

### 완전한 렌더링 파이프라인

```
┌─────────────────────────────────────────────────────────────┐
│ 1. 녹화 (Recording)                                         │
│                                                             │
│    브라우저에서 HUD 조작 (키보드/마우스)                     │
│    ↓                                                        │
│    onStateUpdate() 호출 (33ms 간격)                         │
│    ↓                                                        │
│    hudStateLog[] 배열에 저장                                 │
│    - timestamp, mouse, targets, hitMarkers...               │
│    - animationStarts (UI 등장 시점)                         │
│    ↓                                                        │
│    JSON으로 내보내기                                         │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. Remotion Composition                                     │
│                                                             │
│    <HUDOnlyComposition                                      │
│      hudStateLogJson={JSON.stringify(hudStateLog)}          │
│    />                                                       │
│    ↓                                                        │
│    useCurrentFrame() → timestampMs                          │
│    ↓                                                        │
│    interpolateHUDState(hudStateLog, timestampMs)            │
│    ↓                                                        │
│    ExternalTimestampContext.Provider value={timestampMs}    │
│    ↓                                                        │
│    FrameAnimatedDiv가 progress 계산 후 style 적용           │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. 렌더링 (Remotion CLI)                                    │
│                                                             │
│    npx remotion render HUDOnly \                            │
│      --image-format png \                                   │
│      --sequence \                                           │
│      --output out/frames                                    │
│    ↓                                                        │
│    4K PNG 시퀀스 (투명 배경)                                │
│    out/frames/frame_00000.png ~ frame_XXXXX.png             │
└─────────────────────────────────────────────────────────────┘
```

### 렌더링 품질 옵션

```bash
# PNG 시퀀스 (합성용, 투명 배경)
npx remotion render HUDOnly \
  --image-format png \
  --sequence \
  --width 3840 --height 2160 \
  --output out/frames

# MP4 비디오 (프리뷰용)
npx remotion render HUDOverlay \
  --codec h264 \
  --crf 15 \
  --output out/video.mp4

# ProRes (고화질 마스터용)
npx remotion render HUDOverlay \
  --codec prores \
  --pixel-format yuva444p10le \
  --output out/master.mov
```

---

*이 문서는 Hexa-Tactical HUD 프리셋의 기술적 구조를 설명합니다.  
디자인 가이드는 별도 문서를 참조하세요.*
